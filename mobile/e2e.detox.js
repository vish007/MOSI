// Example Detox E2E skeleton
// detox test --config ios.sim.debug

describe('MOSI App', () => {
  it('should run demo flow and show signal', async () => {
    // await element(by.text('Run MOSI Flow')).tap();
    // await expect(element(by.text('Signal:'))).toBeVisible();
  });
});
